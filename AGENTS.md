# Repository workflow

After every completed upgrade or code change:

1. Run the relevant validation (at minimum `npm run build` for this app).
2. Commit all intended changes with a descriptive commit message.
3. Push the commit to `origin/main`.
4. Verify the GitHub Pages deployment for that commit has completed successfully, then report the result.
